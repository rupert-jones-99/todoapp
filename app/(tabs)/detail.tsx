import { View } from "react-native";

interface DetailProps{
    state: boolean;
    created: Date; 
    name: string;
}
export default function Detail(props: DetailProps){
    return(<View>
        
    </View>)
}